CentOS with NodeJs
==================

Usage
-----

1. run `mkdir /app`;
2. add `package.json` to `/app/`;
3. run `npm install`;
6. add `{current_dir}` to `/app/`;
7. then you could do whatever you want :)


